package shattered.brainsynder.utils;

import org.bukkit.entity.Player;

public class PlayerUtils {
    public static void sendBlood(Player p) {
        /*ParticleMaker redBlock = new ParticleMaker(ParticleMaker.Particle.BLOCK_DUST, 15, 0.5);
        redBlock.setData(Material.REDSTONE_BLOCK);
        ParticleMaker redStone = new ParticleMaker(ParticleMaker.Particle.ITEM_CRACK, 15, 0.5);
        redStone.setData(Material.REDSTONE);
        ParticleMaker red = new ParticleMaker(ParticleMaker.Particle.REDSTONE, 15, 0.5);



        red.sendToLocation(p.getEyeLocation());
        redStone.sendToLocation(p.getEyeLocation());
        redBlock.sendToLocation(p.getEyeLocation());
        SoundMaker.BLOCK_STONE_BREAK.playSound(p.getLocation(), 0.5f, 0.5f);*/
    }
}
